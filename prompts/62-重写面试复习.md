请重写这份协议的面试复习笔记。这是整份重写,不是在旧稿上打补丁。

原因:上一份写早了,当时还缺章节。那三章现在已经写完,必须按全部章节重新出一份。
要求和上次相同:六个区块、中英双语、核心答案可追溯、课程外只放 extension。

== 先做体检,任一失败就停下来告诉我,不要开始写 ==
1. 确认当前目录是本仓库根(能看到 nlnotes\ 和 prompts\)
2. 跑:
     .\.venv\Scripts\python.exe -m nlnotes groups --list
   - `已完成笔记` 必须等于 `章节` 数。不相等 → 原样贴输出并停止,先把缺的章节笔记写完
   - 同时记下分组 id 和 key(下面叫 <GROUP_ID> / <GROUP_KEY>)
3. 不要写章节笔记、不要改代码、不要改 config/、不要下调门禁

== 重建任务包(这一步会把那三章编进 chapters.md) ==
     .\.venv\Scripts\python.exe -m nlnotes groups
然后打开 build/groups/<GROUP_ID>/chapters.md 和 context.json,
确认 chapter_count / 章节清单已经包含全部章(应比旧面试稿多 3 章)。
对不上就停下来把这两个文件的章数贴给我。

== 丢掉旧稿 ==
删除(不要读、不要复用、不要在它上面改):
  build/groups/<GROUP_ID>/OUTPUT/interview.json
旧的 Markdown(`notes\...\00-面试复习-*.md`)不用手工删,build-group 会覆盖。

== 铁律 ==
先读 prompts/50-面试复习.md —— 这是本任务的铁律,**不要**用章节笔记那份 00-system。
再读:
  build/groups/<GROUP_ID>/TASK.md
  build/groups/<GROUP_ID>/chapters.md
  build/groups/<GROUP_ID>/interview.schema.json
需要逐字复制英文原句时,回 build/extract/<pdf_id>/text.md 取。

你的身份:15 年经验的资深网络与安全架构专家 + 大厂技术面试官。
要产出六个区块:知识体系图、跨章必须掌握清单、
高频必考基础/原理题(高分答题模板 + 得分要点)、
场景化面试题(情景模拟 + 解题框架)、
面试官连环追问(每组正好三层递进)、避坑指南。
问题与答案全部中英双语。

**发散的边界:**
- 核心答案、原理、机制、数值、判定顺序 → 必须能在 grounding 指向的原文页找到
- 工程经验、厂商差异、版本演进、跨协议对比 → 只能放 extension_zh / extension_en

**覆盖要求(针对这次漏章):**
- covered_chapters 必须列出 chapters.md 里的全部 pdf_id,一章都不能少
- 那三章新完成的内容必须真正进入知识体系图、必须掌握清单,以及至少各有题目/追问/避坑用到,禁止只把 id 写进 covered_chapters 充数

产出:build/groups/<GROUP_ID>/OUTPUT/interview.json(完整新文件)

写完运行:
  .\.venv\Scripts\python.exe -m nlnotes build-group --group <GROUP_ID>
按 build/reports/group-<GROUP_ID>.json 逐条修到通过(最多 5 轮)。
A003(有章未纳入)这次按错误处理,必须修到没有这条警告。
不要修改门禁配置。

做完汇报:用了几章素材、比旧稿多了哪几章、门禁几轮通过。
