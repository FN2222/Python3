请继续下一批:再连续做完接下来 5 章中文笔记。一章一章来,每章门禁通过再进入下一章。

规则与上一批相同,详见 prompts/60-批量流水作业.md:
- 一律用 .\.venv\Scripts\python.exe,不要 Activate.ps1
- 不要改源 PDF,不要改 config/,不要下调门禁
- 取任务: .\.venv\Scripts\python.exe -m nlnotes next --count 1 --ids-only
  若没有输出,说明未写的章做完了,停下来汇报,不要空转
- 读 TASK.md / source-text.md / figures.md / glossary.md / codeblocks.md / note.schema.json
- 图要打开看,图上的 IP/网段写入 labels_seen
- 写 build/tasks/<ID>/OUTPUT/note.json,然后
  .\.venv\Scripts\python.exe -m nlnotes build --id <ID>
- questions[].type 禁止 command(命令题用 config); points[].kind 禁止 process(用 step)
- 不要把 LSA/DR 展开成本章没出现的全称; quote 必须同页连续
- 密度目标 ≥ 3.0; why_hard_zh 与 how_to_break_zh ≥ 20 字; answer_zh ≥ 25 字

本批只做 5 章,做完停下汇报(每章几轮、有没有反复错误码)。
下一批会自动再发这段话,你不要自己循环完全部剩余章节。
